#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
	Return the player who won this turn (0 if none of them)
*/
int checkVictory()

/*
	Return the list of the coordonates
*/
void checkCatch(c1, c2);

/*
	Check if a move is valid
*/
bool checkMovement(int currentPlayer, coordonates c1, coordonates c2);